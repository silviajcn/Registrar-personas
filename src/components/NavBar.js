import React from 'react';
import { ContainerNavBar, Logo } from '../styles/Styles.elements';

const NavBar = () => {
    return (
        <ContainerNavBar>
            <Logo>Registro</Logo>
        </ContainerNavBar>
    )
};

export default NavBar;